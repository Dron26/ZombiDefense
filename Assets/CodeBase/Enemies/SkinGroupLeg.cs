using UnityEngine;

namespace Enemies
{
    public class SkinGroupLeg : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
